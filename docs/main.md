📌 1. Загальна концепція системи

Назва: ukrPoliceMind
Тип: веб-інформаційна система інтелектуальної консультації
Призначення: надання структурованих інформаційних консультацій з питань правоохоронної діяльності

Система реалізує два режими роботи:

Чат-консультація — вільний текстовий запит.

Майстер консультації (Wizard) — керований сценарій з уточнювальними питаннями.

Система також містить адміністративну підсистему для керування сценаріями та параметрами LLM.

📌 2. Архітектура системи
2.1 Архітектурний стиль

Клієнт-серверна архітектура

SPA (Single Page Application)

REST API + SSE для потокової передачі відповіді

Монорепозиторій

2.2 Технологічний стек
Frontend

React + TypeScript

Vite

Tailwind CSS

shadcn/ui + Radix UI

TanStack Query

React Hook Form + Zod

react-markdown

react-virtuoso

Backend

NestJS + TypeScript

PostgreSQL

Prisma ORM

JWT (access + refresh)

Server-Sent Events (SSE)

nestjs-pino (логування)

2.3 Структура монорепозиторію
ukrPoliceMind/
│
├── client (React)
│── server (NestJs)
├── packages/
│   └── shared/   (типи, DTO, enum)