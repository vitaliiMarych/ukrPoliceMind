# 🚀 Quick Start Guide - ukrPoliceMind

## Швидкий старт за 5 хвилин

### 1️⃣ Підготовка

```bash
# Клонування репозиторію
git clone https://github.com/vitaliiMarych/ukrPoliceMind.git
cd ukrPoliceMind
```

### 2️⃣ Backend Setup

```bash
cd server

# Встановлення залежностей
npm install

# Налаштування .env
cp .env.example .env

# ⚠️ ВАЖЛИВО: Відредагуйте .env файл:
# - Додайте DATABASE_URL (або використайте docker-compose)
# - Додайте JWT_SECRET (будь-який випадковий рядок)
# - Додайте GEMINI_API_KEY (отримати на https://makersuite.google.com/app/apikey)

# Запуск PostgreSQL (якщо немає локально)
docker-compose up -d

# Міграції БД
npx prisma migrate dev

# Seed тестовими даними
npm run prisma:seed

# Запуск backend
npm run start:dev
```

✅ Backend готовий на http://localhost:3000

**Тестові акаунти:**
- Admin: `admin@ukrpolicemind.com` / `admin123`
- User: `user@ukrpolicemind.com` / `user123`

### 3️⃣ Frontend Setup

```bash
# Відкрити нове вікно терміналу
cd client

# Встановлення залежностей
npm install

# Налаштування .env (опціонально, якщо backend не на localhost:3000)
cp .env.example .env

# Запуск frontend
npm run dev
```

✅ Frontend готовий на http://localhost:5173

### 4️⃣ Перевірка роботи

1. Відкрийте http://localhost:5173
2. Натисніть "Login"
3. Увійдіть як `user@ukrpolicemind.com` / `user123`
4. Спробуйте Chat або Wizard режим!

---

## 🔑 Отримання Gemini API Key

1. Перейдіть на https://makersuite.google.com/app/apikey
2. Натисніть "Create API key"
3. Скопіюйте ключ
4. Додайте в `server/.env`:
   ```
   GEMINI_API_KEY="ваш-ключ-тут"
   ```

---

## 🐛 Troubleshooting

### Backend не запускається
```bash
# Перевірте чи запущений PostgreSQL
pg_isready

# Або перезапустіть Docker
docker-compose restart
```

### Помилка міграцій
```bash
# Скиньте БД
npx prisma migrate reset

# Знову запустіть міграції
npx prisma migrate dev
```

### Frontend не може з'єднатися з Backend
1. Переконайтеся що backend запущений на http://localhost:3000
2. Перевірте `client/.env` - VITE_API_URL повинен бути правильним

---

## 📚 Детальна документація

- [Повна документація проекту](README.md)
- [Backend документація](server/README_RUN.md)
- [API документація](server/API_DOCS.md)
- [Frontend документація](client/README.md)

---

## 🎯 Що далі?

### Для розробки:
- Вивчіть [API документацію](server/API_DOCS.md)
- Подивіться на структуру проекту
- Спробуйте додати нову wizard категорію через admin панель

### Для production:
- Змініть JWT_SECRET на унікальний
- Налаштуйте правильний DATABASE_URL
- Використайте реальний SMTP для email (якщо потрібно)
- Налаштуйте CORS для вашого домену
- Використайте HTTPS

---

**Успіхів! 🎉**
